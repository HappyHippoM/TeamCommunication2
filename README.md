# Multiplayer Figure-Finding Game

Prototype of online logic game for 5-6 players with private communication and role-based tasks.
See full description in ChatGPT documentation.

## Run locally
cd server && npm install && npm start
cd client && npm install && npm run start
